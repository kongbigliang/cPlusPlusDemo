#include<cstdio>
#include<cstring>
using namespace std;
int main()
{
	char[200] s1, s2;
	gets(s1);
	gets(s2);
	int len1 = strlen(s1);
	int len2 = strlen(s2);
	for (int i = 0; i < len1; i++)
	{
		for (j = 0; )
	}
	return 0;
}
